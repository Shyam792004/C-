
using System;
using System.Text;
using System.Collections.Generic;
using System.Linq;
class HelloWorld
{
    static void LongestWord(String s)
    {
       string [] arr=s.Split(' ');
       int max=int.MinValue;
       string ans=arr[0];
       string second="";
       for(int i=0;i<arr.Length;i++)
       {
           if(max<arr[i].Length)
           {
               max=arr[i].Length;
               second=ans;
               ans=arr[i];
           }
           else if(second.Length<arr[i].Length && arr[i]!=ans)
           {
               second=arr[i];
           }
       }
       Console.WriteLine(ans);
       Console.Write(second);
       
    }
    
   static void Main() 
   {

      string s=Console.ReadLine();
      LongestWord(s);
      
   }
}
