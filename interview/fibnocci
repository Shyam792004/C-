
using System;
using System.Text;
using System.Collections.Generic;
class HelloWorld
{
    static void fib(int n)
    {
       int a=0,b=1;
       for(int i=0;i<n;i++)
       {
          Console.Write(a+" ");
           int c=a+b;
           a=b;
           b=c;
       }

    }
    
    
   static void Main() 
   {
       int a=int.Parse(Console.ReadLine());
       fib(a);
   }
}
