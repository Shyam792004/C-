    static void Reverse(string s)
    {
        char [] c=new char[s.Length];
        int n=s.Length;
        for(int i=0;i<n;i++)
        {
            c[i]=s[n-i-1];
        }
        Console.Write(c);
    }
